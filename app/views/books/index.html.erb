<script type="text/javascript">
    $(function(){
      $('#bookstore').click(function(event){
          $('#modal_dialog').dialog({
              width: 600,
              height: 150,
              autoOpen: true,
              modal: true//,
              //drag: function(event) {
              //    //$.get(<%= url_for :action => :index %>)
              //    window.location.href = <%= url_for :action => :index %>;
              //}

              //show: 'bounce',
              //hide: 'explode',

          }).dialog('open');

          $('#modal_dialog').dialog({
              close: function(event) {
                  window.location.href = '<%= url_for :action => :index %>';

              }
          });

      });

    });
</script>
<div id="modal_dialog"></div>
<h1>Listing books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
<%= link_to 'bookstore',bookstores_path,:remote => true ,:id=>:bookstore%>

